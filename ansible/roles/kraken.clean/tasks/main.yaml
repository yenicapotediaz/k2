---
- name: clean the generated folder
  file:
    path: "{{ config_base | expanduser }}/{{ cluster.name }}"
    state: absent
  with_items:
    - "{{ kraken_config.clusters }}"
  loop_control:
    loop_var: cluster
