---
- include: generate-oidc-cert.yaml
  with_items:
    - "{{ kraken_config.clusters }}"
  loop_control:
    loop_var: a_cluster
  when: (a_cluster.kubeAuth.authn.oidc is defined) and (kraken_action == 'up') and (a_cluster.clusterServices is defined)

- include: kill-dns.yaml
  with_items:
    - "{{ kraken_config.clusters }}"
  loop_control:
    loop_var: a_cluster
  when: kraken_action == 'down'
  static: no
- include: run-dns.yaml
  with_items:
    - "{{ kraken_config.clusters }}"
  loop_control:
    loop_var: a_cluster
  when: kraken_action == 'up'
  static: no

- include: kill-services.yaml
  with_items:
    - "{{ kraken_config.clusters }}"
  loop_control:
    loop_var: a_cluster
  when: a_cluster.clusterServices is defined

- include: run-services.yaml
  with_items:
    - "{{ kraken_config.clusters }}"
  loop_control:
    loop_var: a_cluster
  when: (kraken_action == 'up') and (a_cluster.clusterServices is defined)
  static: no
